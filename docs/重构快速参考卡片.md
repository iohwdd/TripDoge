# 重构快速参考卡片

**版本**: v1.0.0  
**创建日期**: 2025-01-24  
**最后更新**: 2025-01-24  
**编写人**: AI Assistant  
**审核人**: 待审核  
**用途**: 快速查找变量映射关系

**最新验证**（2025-01-24）:
- ✅ `frontend/src/styles/variables.css` 包含 105 个变量，已通过 `grep -E "^  --" … | wc -l` 验证
- ✅ 代码已运行 `npm run format` / `npm run format:check`
- 🔁 如本地新增修改，请先执行 `npm run format:check`

> **如何使用**：
> 1. 先阅读《重构当前状态说明》和《重构技术实施指南》了解流程。
> 2. 在编辑 CSS 时通过此卡片快速查找尺寸/颜色变量。
> 3. 若需新增变量，请更新 `variables.css` 并在此卡片补充映射。

### 变量文件快速验证

```bash
# 统计 Design Tokens 数量（期望 105）
cd frontend
grep -E "^  --" src/styles/variables.css | wc -l
```

---

## 📏 尺寸变量快速查找

### 宽度（Width）

| 原值 | 变量 | 用途 |
|------|------|------|
| `280px` | `var(--layout-sidebar-width)` | 侧边栏宽度 |
| `320px` | `var(--layout-sidebar-width-tablet)` | 平板侧边栏 |
| `68px` | `var(--sidebar-width-desktop)` | 桌面侧边栏 |
| `60px` | `var(--sidebar-width-tablet)` | 平板侧边栏 |
| `800px` | `var(--layout-content-max-width)` | 内容最大宽度 |
| `1200px` | `var(--layout-container-max-width)` | 容器最大宽度 |

### 高度（Height）

| 原值 | 变量 | 用途 |
|------|------|------|
| `36px` | `var(--button-height-md)` | 按钮高度 |
| `44px` | `var(--button-height-lg)` | 大按钮高度 |
| `28px` | `var(--button-height-sm)` | 小按钮高度 |
| `180px` | `var(--spacing-xxxl)` (近似) | 卡片最小高度 |

### 图标尺寸（Icon Size）

| 原值 | 变量 | 用途 |
|------|------|------|
| `64px` | `var(--icon-size-xxl)` | 超大图标 |
| `48px` | `var(--icon-size-xl)` | 大图标 |
| `40px` | `var(--icon-size-xl)` | 图标 |
| `36px` | `var(--icon-size-lg)` | 中等图标 |
| `20px` | `var(--icon-size-md)` | 小图标 |
| `12px` | `var(--icon-size-sm)` | 超小图标 |
| `8px` | `var(--icon-size-xs)` | 最小图标 |

### 头像尺寸（Avatar Size）

| 原值 | 变量 | 用途 |
|------|------|------|
| `60px` | `var(--avatar-size-lg)` | 大头像 |
| `40px` | `var(--avatar-size-md)` | 中等头像 |
| `32px` | `var(--avatar-size-sm)` | 小头像 |

### 间距（Spacing）

| 原值 | 变量 | 用途 |
|------|------|------|
| `64px` | `var(--spacing-xxxl)` | 超大间距 |
| `48px` | `var(--spacing-xxl)` | 大间距 |
| `32px` | `var(--spacing-xl)` | 大间距 |
| `24px` | `var(--spacing-lg)` | 中等间距 |
| `20px` | `var(--spacing-lg)` | 中等间距（近似） |
| `16px` | `var(--spacing-md)` | 标准间距 |
| `12px` | `var(--spacing-sm)` | 小间距 |
| `10px` | `var(--spacing-sm)` | 小间距（近似） |
| `8px` | `var(--spacing-sm)` | 小间距 |
| `4px` | `var(--spacing-xs)` | 超小间距 |
| `2px` | `var(--spacing-xxs)` | 最小间距 |

### 字体大小（Font Size）

| 原值 | 变量 | 用途 |
|------|------|------|
| `64px` | `var(--font-size-xxxxxl)` | 超大标题 |
| `48px` | `var(--font-size-xxxxl)` | 大标题 |
| `32px` | `var(--font-size-xxxl)` | 标题 |
| `24px` | `var(--font-size-xxl)` | 大标题 |
| `20px` | `var(--font-size-xl)` | 标题 |
| `18px` | `var(--font-size-lg)` | 大文字 |
| `16px` | `var(--font-size-md)` | 标准文字 |
| `14px` | `var(--font-size-sm)` | 小文字 |
| `13px` | `var(--font-size-sm)` | 小文字（近似） |
| `12px` | `var(--font-size-xs)` | 超小文字 |
| `11px` | `var(--font-size-xxs)` | 最小文字 |

### 圆角（Border Radius）

| 原值 | 变量 | 用途 |
|------|------|------|
| `50%` | `var(--border-radius-full)` | 圆形 |
| `16px` | `var(--border-radius-xxl)` | 大圆角 |
| `12px` | `var(--border-radius-xl)` | 中等圆角 |
| `8px` | `var(--border-radius-lg)` | 标准圆角 |
| `6px` | `var(--border-radius-md)` | 小圆角 |
| `4px` | `var(--border-radius-md)` | 小圆角 |
| `2px` | `var(--border-radius-sm)` | 超小圆角 |
| `1px` | `var(--border-radius-xs)` | 最小圆角 |

---

## 🎨 颜色变量快速查找

### 背景颜色（Background）

| 原值 | 变量 | 用途 |
|------|------|------|
| `#fff` | `var(--bg-color-base)` | 白色背景 |
| `#fdfdfd` | `var(--bg-color-quaternary)` | 最浅灰背景 |
| `#fafafa` | `var(--bg-color-tertiary)` | 浅灰背景 |
| `#f5f5f5` | `var(--bg-color-secondary)` | 灰色背景 |
| `#e6e6e6` | `var(--bg-color-disabled)` | 禁用背景 |
| `#e6f7ff` | `var(--bg-color-active)` | 激活背景 |
| `rgba(0, 0, 0, 0.04)` | `var(--bg-color-hover)` | 悬停背景 |
| `#1e1f22` | `var(--bg-color-dark)` | 深色背景 |
| `#001529` | `var(--bg-color-dark)` | 深色背景 |

### 文字颜色（Text）

| 原值 | 变量 | 用途 |
|------|------|------|
| `#262626` | `var(--text-color-heading)` | 标题文字 |
| `rgba(0, 0, 0, 0.85)` | `var(--text-color-primary)` | 主要文字 |
| `rgba(0, 0, 0, 0.65)` | `var(--text-color-secondary)` | 次要文字 |
| `#8c8c8c` | `var(--text-color-secondary)` | 次要文字 |
| `rgba(0, 0, 0, 0.45)` | `var(--text-color-tertiary)` | 第三级文字 |
| `rgba(0, 0, 0, 0.25)` | `var(--text-color-quaternary)` | 第四级文字 |
| `#bfbfbf` | `var(--text-color-placeholder)` | 占位文字 |
| `#fff` | `var(--text-color-inverse)` | 反色文字 |

### 主题颜色（Theme）

| 原值 | 变量 | 用途 |
|------|------|------|
| `#1890ff` | `var(--color-primary)` | 主色 |
| `#40a9ff` | `var(--link-hover-color)` | 链接悬停 |
| `#52c41a` | `var(--color-success)` | 成功色 |
| `#faad14` | `var(--color-warning)` | 警告色 |
| `#f5222d` | `var(--color-error)` | 错误色 |
| `rgba(24, 144, 255, 0.1)` | `var(--color-primary-light)` | 主色浅色 |
| `rgba(24, 144, 255, 0.02)` | `var(--color-primary-lighter)` | 主色更浅 |
| `rgba(82, 196, 26, 0.1)` | `var(--color-success-light)` | 成功色浅色 |

### 边框颜色（Border）

| 原值 | 变量 | 用途 |
|------|------|------|
| `rgba(0, 0, 0, 0.06)` | `var(--border-color-base-alpha)` | 标准边框 |
| `rgba(0, 0, 0, 0.04)` | `var(--border-color-light-alpha)` | 浅色边框 |
| `#d9d9d9` | `var(--border-color-dashed)` | 虚线边框 |
| `#e8e8e8` | `var(--border-color-light)` | 浅色边框 |

---

## 📱 响应式断点

### 标准断点

| 断点 | 值 | 用途 |
|------|-----|------|
| 超小屏 | `< 360px` | 超小手机 |
| 小屏 | `< 480px` | 小手机 |
| 移动端 | `< 768px` | 手机 |
| 平板端 | `768px - 1024px` | 平板 |
| 桌面端 | `> 1024px` | 桌面 |
| 大桌面 | `> 1200px` | 大桌面 |
| 超大桌面 | `> 1600px` | 超大桌面 |

### 使用示例

```css
/* 移动优先 */
.component {
  padding: var(--spacing-md);
}

/* 平板端 */
@media (min-width: 769px) {
  .component {
    padding: var(--spacing-lg);
  }
}

/* 桌面端 */
@media (min-width: 1025px) {
  .component {
    padding: var(--spacing-xl);
  }
}
```

---

## 🔍 常用组合模式

### 卡片样式

```css
.card {
  padding: var(--spacing-card-padding);
  border-radius: var(--border-radius-lg);
  background-color: var(--bg-color-base);
  box-shadow: var(--box-shadow-sm);
}
```

### 按钮样式

```css
.button {
  height: var(--button-height-md);
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--border-radius-lg);
  background-color: var(--color-primary);
  color: var(--text-color-inverse);
}
```

### 输入框样式

```css
.input {
  padding: var(--spacing-input-padding);
  border-radius: var(--border-radius-md);
  border: 1px solid var(--border-color-base-alpha);
  font-size: var(--font-size-md);
}
```

### 标题样式

```css
.heading {
  font-size: var(--font-size-xl);
  font-weight: 600;
  color: var(--text-color-heading);
  margin-bottom: var(--spacing-md);
  line-height: var(--line-height-tight);
}
```

---

## ⚡ 快速替换技巧

### VS Code 查找替换

1. **打开查找替换** (`Ctrl+H` / `Cmd+H`)
2. **启用正则表达式** (点击 `.*` 图标)
3. **使用以下模式**：

#### 替换硬编码 px 值
```
查找: width:\s*(\d+)px
替换: width: var(--layout-width-$1)
```

#### 替换硬编码颜色
```
查找: background-color:\s*#([0-9a-fA-F]{6})
替换: background-color: var(--bg-color-$1)
```

**注意**: 需要手动检查替换结果，确保变量名称正确。

### 批量替换建议

不建议使用全自动替换，建议：
1. 逐个文件手动替换
2. 每次替换后测试
3. 确保功能正常

---

## 📋 检查清单

### 替换前检查
- [ ] 备份原文件
- [ ] 了解变量系统
- [ ] 准备测试环境

### 替换中检查
- [ ] 逐个替换
- [ ] 保持功能不变
- [ ] 及时测试

### 替换后检查
- [ ] 功能正常
- [ ] 样式正常
- [ ] 响应式正常
- [ ] 浏览器兼容

---

## 🆘 遇到问题？

### 问题1: 找不到对应的变量
**解决**: 
1. 检查 `variables.css` 是否有类似变量
2. 如果没有，评估是否需要添加
3. 临时可以使用 `calc()` 计算

### 问题2: 变量值不合适
**解决**:
1. 检查变量定义是否正确
2. 考虑是否需要调整变量值
3. 或者创建新的语义化变量

### 问题3: 响应式布局异常
**解决**:
1. 检查媒体查询是否正确
2. 检查变量在不同断点的值
3. 可能需要添加响应式变量

---

**文档状态**: 📝 参考文档  
**最后更新**: 2025-01-24
