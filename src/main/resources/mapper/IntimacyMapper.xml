<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripdog.mapper.IntimacyMapper">

    <resultMap id="BaseResultMap" type="com.tripdog.model.entity.IntimacyDO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="uid" property="uid" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="intimacy" property="intimacy" jdbcType="INTEGER"/>
        <result column="last_msg_time" property="lastMsgTime" jdbcType="TIMESTAMP"/>
        <result column="last_daily_bonus_date" property="lastDailyBonusDate" jdbcType="DATE"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="selectByUserAndRole" resultMap="BaseResultMap">
        SELECT * FROM t_intimacy WHERE uid = #{uid} AND role_id = #{roleId}
    </select>

    <insert id="insert" parameterType="com.tripdog.model.entity.IntimacyDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_intimacy (uid, role_id, intimacy, last_msg_time, last_daily_bonus_date, created_at, updated_at)
        VALUES (#{uid}, #{roleId}, #{intimacy}, #{lastMsgTime}, #{lastDailyBonusDate}, NOW(), NOW())
    </insert>

    <update id="updateIntimacy">
        UPDATE t_intimacy
        SET intimacy = #{intimacy},
            last_msg_time = #{lastMsgTime},
            last_daily_bonus_date = #{lastDailyBonusDate},
            updated_at = NOW()
        WHERE uid = #{uid} AND role_id = #{roleId}
    </update>

    <select id="listInactiveSince" resultMap="BaseResultMap">
        <![CDATA[
        SELECT * FROM t_intimacy
        WHERE last_msg_time IS NULL OR last_msg_time <= #{threshold}
        ]]>
    </select>
</mapper>

